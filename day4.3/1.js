const numbers = [2, 5, 3, 7, 22, 44, 55, 51, 16, 27]
let valor = 27;

if (numbers.includes(valor)){
        console.log(numbers.indexOf(valor))
    } else {
        console.log('Elemento não encontrado no array')
    }
